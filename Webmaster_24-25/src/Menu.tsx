import React from "react";
import ReactDOM from "react-dom/client";
import Menu_Render from "./Menu_Render";
import "./index.css";

function menu() {
	return (
		<React.StrictMode>
			<Menu_Render />
		</React.StrictMode>
	);
}
